# Scenario greenfield-feature.v1

Starting from the frozen v8 baseline, add a small user-visible command that reports the active Legion version, the configured command roots, and whether required workflow directories exist. Keep the command implementation local to existing command/skill conventions and preserve all existing v8 commands.

Expected public verification: run the existing v8 validation command set plus the scenario-specific smoke or regression evidence named in the run manifest.
