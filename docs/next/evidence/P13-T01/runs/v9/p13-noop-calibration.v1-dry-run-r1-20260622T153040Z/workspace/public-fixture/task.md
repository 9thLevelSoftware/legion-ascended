# Scenario noop-calibration.v1

Use this fixture only to verify the P00-T05 harness. The runner should copy the public input, write a manifest, redact the configured canary, and allow the grader to emit a scaffold score without invoking v8.
