schema_version: 1
scenario_id: greenfield-feature.v1
visible_to_worker: false
critical_assertions:
  - existing commands remain listed and runnable
  - new command reports package version from package.json rather than a hard-coded value
  - command roots are discovered from current filesystem state
calibration:
  known_good: Runtime-derived metadata and preserved existing validation.
  known_bad: Hard-coded version or removed existing command.
