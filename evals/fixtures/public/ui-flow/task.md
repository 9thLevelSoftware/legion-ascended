# Scenario ui-flow.v1

Starting from the frozen v8 baseline, improve the terminal flow for a command that asks the user to choose between continuing, pausing, or cancelling a workflow. Keep keyboard-only operation, make the default choice explicit, and preserve non-interactive behavior.

Expected public verification: run the existing v8 validation command set plus the scenario-specific smoke or regression evidence named in the run manifest.
