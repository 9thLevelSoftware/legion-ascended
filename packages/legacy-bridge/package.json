{
  "name": "@legion/legacy-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm --filter @legion/artifacts build && pnpm run build && node --test --test-reporter=spec \"test/**/*.test.mjs\""
  },
  "dependencies": {
    "@legion/artifacts": "workspace:*",
    "@legion/protocol": "workspace:*",
    "yaml": "^2.8.1"
  },
  "devDependencies": {
    "@types/node": "^24.0.0"
  }
}
