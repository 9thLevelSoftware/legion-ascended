{
  "name": "@legion/protocol",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "generate:schemas": "pnpm run build && node scripts/generate-primitives-json-schema.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm run build && node --test --test-reporter=spec \"test/**/*.test.mjs\""
  },
  "dependencies": {
    "zod": "^4.4.3"
  }
}
