{
  "name": "@legion/board",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm --filter @legion/board-store build && pnpm --filter @legion/core build && pnpm run build && node --test --test-reporter=spec \"test/**/*.test.mjs\""
  },
  "dependencies": {
    "@legion/board-store": "workspace:*",
    "@legion/core": "workspace:*",
    "@legion/protocol": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.0.0"
  }
}
